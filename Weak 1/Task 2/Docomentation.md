# Task 2 Documentation 
**1. Random Number Generation:** Uses random.sample(0, 9) to pick a hidden number.

**2. Input Validation:** Uses a try-except block to gracefully handle text inputs instead of crashing.

**3. Attempt Counter:** Keeps track of how many tries it takes to find the correct number.

**4. Replay Loop:** Asks the player if they want to play another round when they finish.